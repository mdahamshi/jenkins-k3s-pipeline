apiVersion: v1
kind: Service
metadata:
  name: jenkins-k3s-app
  namespace: default
spec:
  selector:
    app: jenkins-k3s-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: NodePort
